apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: learning-platform
    component: backend
spec:
  type: ClusterIP  # Internal only - not accessible from outside cluster
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: learning-platform
    component: backend
